# 🧠 SHL Assessment Recommendation System

A GenAI-powered web app that recommends relevant SHL assessments based on job descriptions or queries.

### Features
- Embedding-based similarity search using SentenceTransformer (`all-MiniLM-L6-v2`)
- FAISS index for efficient retrieval
- Streamlit UI for easy interaction

### Run locally
```bash
streamlit run streamlit_app.py
